# @slotclientengine/platformbootstrap

Protocol-neutral, per-instance platform bootstrap contracts. Snapshots are
validated, copied and deeply frozen; credentials, transports and UI state are
intentionally outside this package.
