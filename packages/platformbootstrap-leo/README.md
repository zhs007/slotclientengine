# @slotclientengine/platformbootstrap-leo

Strict, per-instance Leo launcher bootstrap. It validates launcher parameters,
loads config/translations/settings with cancellation, and returns an immutable
credential-free platform snapshot. It never creates a live session or UI.
