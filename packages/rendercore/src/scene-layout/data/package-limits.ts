export const SCENE_LAYOUT_PRODUCTION_ZIP_LIMITS = Object.freeze({
  maxEntries: 4096,
  maxCompressedBytes: 200 * 1024 * 1024,
  maxFileBytes: 50 * 1024 * 1024,
  maxTotalBytes: 500 * 1024 * 1024,
});
