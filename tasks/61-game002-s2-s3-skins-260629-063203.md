# game002 s2/s3 skins 完成报告

## 1. 基本信息

- UTC 时间戳：260629-063203
- 执行人：Codex
- 对应计划：`tasks/61-game002-s2-s3-skins.md`
- 结论：已完成代码、资源生成、文档同步和任务面验收；根级 `pnpm format:check` 仍因历史非本任务格式问题失败，详见第 8 节。

## 2. 资源盘点和生成结果

补齐后已确认：

- `assets/game002-s2/CN.png` 存在，尺寸 `200 x 200`。
- `assets/game002-s2/bg.png` 尺寸 `2000 x 2000`。
- `assets/game002-s3/bg.jpg` 尺寸 `2000 x 2000`。
- `assets/game002-s2` / `assets/game002-s3` 的普通 symbol PNG 均为 `200 x 200`。

已生成：

```text
assets/game002-s2/CN.disabled.png
assets/game002-s2/CN.spinBlur.png
assets/game002-s2/CO.disabled.png
assets/game002-s2/CO.spinBlur.png
assets/game002-s2/H1.disabled.png
assets/game002-s2/H1.spinBlur.png
assets/game002-s2/H2.disabled.png
assets/game002-s2/H2.spinBlur.png
assets/game002-s2/L1.disabled.png
assets/game002-s2/L1.spinBlur.png
assets/game002-s2/L2.disabled.png
assets/game002-s2/L2.spinBlur.png
assets/game002-s2/L3.disabled.png
assets/game002-s2/L3.spinBlur.png
assets/game002-s2/L4.disabled.png
assets/game002-s2/L4.spinBlur.png
assets/game002-s2/WL.disabled.png
assets/game002-s2/WL.spinBlur.png
assets/game002-s2/symbol-state-textures.manifest.json

assets/game002-s3/AF.disabled.png
assets/game002-s3/AF.spinBlur.png
assets/game002-s3/CM.disabled.png
assets/game002-s3/CM.spinBlur.png
assets/game002-s3/CN.disabled.png
assets/game002-s3/CN.spinBlur.png
assets/game002-s3/CO.disabled.png
assets/game002-s3/CO.spinBlur.png
assets/game002-s3/H1.disabled.png
assets/game002-s3/H1.spinBlur.png
assets/game002-s3/H2.disabled.png
assets/game002-s3/H2.spinBlur.png
assets/game002-s3/L1.disabled.png
assets/game002-s3/L1.spinBlur.png
assets/game002-s3/L2.disabled.png
assets/game002-s3/L2.spinBlur.png
assets/game002-s3/L3.disabled.png
assets/game002-s3/L3.spinBlur.png
assets/game002-s3/L4.disabled.png
assets/game002-s3/L4.spinBlur.png
assets/game002-s3/WL.disabled.png
assets/game002-s3/WL.spinBlur.png
assets/game002-s3/WM.disabled.png
assets/game002-s3/WM.spinBlur.png
assets/game002-s3/symbol-state-textures.manifest.json
```

manifest 复核：

- 两个 manifest `version` 均为 `1`。
- `states` 均为 `spinBlur,disabled`。
- 每个 symbol 都有 `normal`、`spinBlur`、`disabled`、`scale: 1`。
- `bg` 没有进入任一 manifest。

## 3. 最终 skin 映射

`apps/game002` 新增：

```text
skin=4 -> assets/game002-s2/bg.png + assets/game002-s2 symbols
skin=5 -> assets/game002-s3/bg.jpg + assets/game002-s3 symbols
```

两套新 skin 继续复用：

- `assets/gamecfg002/gameconfig.json`
- `reels-001` 本地公开轮带
- URL 传入的 live `gamecode`
- 同一套 live/spin/collect/grid-cell reel runtime 流程

`skin=4`：

- display symbols：`WL,H1,H2,L1,L2,L3,L4,CN,CO`
- empty symbols：`BN`
- manifest scale：全部 `1`
- 缺 `WM`、`CM`、`AF` 时显式失败

`skin=5`：

- display symbols：`WL,H1,H2,L1,L2,L3,L4,WM,CN,CM,CO,AF`
- empty symbols：`BN`
- manifest scale：全部 `1`
- `BN` 作为显式 empty symbol，不要求 `BN.png`

## 4. 布局和 focus

按用户要求，新 skin 与旧 `skin=2` / `skin=3` 配置一致，但源码中逐 skin 显式配置：

```text
skin=4 gridLayout = GAME002_SKIN4_GRID_LAYOUT
skin=5 gridLayout = GAME002_SKIN5_GRID_LAYOUT
boardFrame: x=637.5, y=330, width=720, height=1080
cell: 120 x 120

skin=4 focusRegion = GAME002_SKIN4_FOCUS_REGION
skin=5 focusRegion = GAME002_SKIN5_FOCUS_REGION
focusRegion: x=637.5, y=330, width=720, height=1080
```

没有用默认值或 board frame 推导 focus。

## 5. 修改文件摘要

主要实现：

- `apps/game002/src/skin-id.ts`
- `apps/game002/src/skin-config.ts`
- `apps/game002/src/game-layout.ts`
- `apps/game002/scripts/verify-static-dist.mjs`
- `apps/symbolsviewer/src/symbol-set-config.ts`
- `apps/symbolsviewer/src/symbol-animation-config.ts`

测试和文档：

- `apps/game002/tests/*.test.ts`
- `apps/symbolsviewer/tests/symbol-set-config.test.ts`
- `apps/game002/README.md`
- `apps/symbolsviewer/README.md`
- `agents.md`
- `AGENTS.md` 内容已与 `agents.md` 保持一致；当前 git 跟踪的是小写 `agents.md`。

格式边界：

- 新增 `apps/symbolsviewer/.prettierignore`
- 新增若干 workspace package `.prettierignore`，统一忽略 `dist`、`coverage`、`node_modules`，避免 `prettier --check .` 扫生成物。
- 机械格式化 `apps/symbolsviewer` 既有配置文件和 `src/viewer-sequence.ts`，不改行为。

## 6. 已执行验收命令

资源生成：

```bash
pnpm --filter @slotclientengine/rendercore generate:symbol-state-textures -- --input-dir assets/game002-s2 --output-dir assets/game002-s2 --symbols WL,H1,H2,L1,L2,L3,L4,CN,CO --scale 1
pnpm --filter @slotclientengine/rendercore generate:symbol-state-textures -- --input-dir assets/game002-s3 --output-dir assets/game002-s3 --symbols WL,H1,H2,L1,L2,L3,L4,WM,CN,CM,CO,AF --scale 1
```

首次未带 `CI=true` 时触发 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`；按计划加 `CI=true` 重跑后均通过。

通过：

```bash
CI=true pnpm --filter @slotclientengine/rendercore test
CI=true pnpm --filter symbolsviewer test
CI=true pnpm --filter symbolsviewer typecheck
CI=true pnpm --filter symbolsviewer lint
CI=true pnpm --filter symbolsviewer build
CI=true pnpm --filter symbolsviewer format:check
CI=true pnpm --filter game002 test
CI=true pnpm --filter game002 typecheck
CI=true pnpm --filter game002 lint
CI=true pnpm --filter game002 build
CI=true pnpm --filter game002 release:check
CI=true pnpm --filter game002 format:check
CI=true pnpm exec prettier --check <本任务修改文件列表>
git diff --check
cmp -s AGENTS.md agents.md
```

测试结果摘要：

- `@slotclientengine/rendercore test`：20 files / 112 tests passed
- `symbolsviewer test`：2 files / 25 tests passed
- `game002 test`：10 files / 81 tests passed
- `game002 release:check`：`game002 static dist check passed.`

## 7. 浏览器验收

浏览器 live 验收未执行，原因是当前没有可用 live 参数。未把单元测试或 release check 写成 live 验收通过。

建议后续用计划中的 URL 分别检查：

```text
skin=4
skin=5
```

## 8. 未通过项和原因

根级命令：

```bash
CI=true pnpm format:check
```

仍未通过。剩余失败来自非本任务改动范围的历史源码格式问题，例如：

- `apps/gengameconfig/src/*.ts`
- `packages/logiccore/src/*.ts`
- `apps/spine2pixiani-demo/*`
- `apps/victoryani-demo/*`
- `apps/gameclientcli/src/gameplay-stats.ts`
- `apps/gameclientcli/tests/*.ts`

本任务已修复生成物被 format 扫描的问题，但未把这些无关源码批量格式化进 61 任务，避免扩大 diff 和行为审查范围。

## 9. pnpm lock 和工作区

`pnpm-lock.yaml` 未变化。

最终 `git status --short --untracked-files=all` 摘要：

- 已修改：`agents.md`、`apps/game002/*`、`apps/symbolsviewer/*`
- 新增未跟踪：`assets/game002-s2/*`、`assets/game002-s3/*`
- 新增未跟踪：多个 `.prettierignore`
- 新增未跟踪：`tasks/61-game002-s2-s3-skins.md`、前置阻塞报告、本完成报告

## 10. 二次遗漏检查

- `assets/game002-s2/bg.png` 没有进入 manifest、viewer catalog 或 runtime symbol modules。
- `assets/game002-s2/CN.png`、`CN.spinBlur.png`、`CN.disabled.png` 和 manifest 条目已存在。
- `game002-s2` / `game002-s3` manifest 每个 symbol 都显式 `scale: 1`。
- `symbolsviewer` selector 配置包含 `game002-s2` / `game002-s3`。
- `game002` query parser 接受 `skin=4` / `skin=5`，拒绝其它非法 skin。
- `game002` 的 `skin=4` / `skin=5` 没有借用旧 symbol 目录。
- `skin=4` / `skin=5` 都显式配置 `gridLayout` 和 `focusRegion`。
- `verify-static-dist.mjs` 覆盖五套 skin，并检查 PNG/JPEG 背景尺寸和 manifest scale。
- README、`agents.md`、`AGENTS.md` 已同步。
- 未修改 live/server/gamecode/bet/collect 合同。
- 未放宽 production fail-fast 合同。
