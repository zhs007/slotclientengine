# 49-game002-bootstrap 执行报告

- 报告时间：260625-090455 UTC
- 计划文件：`tasks/49-game002-bootstrap.md`
- 执行结论：已完成并通过严格验收。

## 实施范围

1. 新增 `apps/game002` Vite 应用，按 game002 资源独立配置：
   - 背景：`assets/game002/bg.jpg`
   - 逻辑配置：`assets/gamecfg002/gameconfig.json`
   - 符号资源：`assets/symbols002/*`
   - 设计尺寸：`1125 x 2000`
   - 棋盘：初版按计划使用 `x=200, y=400, width=900, height=1350`；复验后修正为背景真实棋盘 `x=200, y=330, width=720, height=1080`
   - reel：`6 x 9`，cell 从初版 `150` 修正为真实棋盘 pitch `120`
2. `game002` 只通过 `@slotclientengine/gameframeworks` 接入 slot facade，不直接依赖 `@slotclientengine/uiframeworks`、`@slotclientengine/netcore`、`@slotclientengine/logiccore`。
3. `game002` 使用 `@slotclientengine/rendercore` 创建真实 Pixi/reel 渲染层，未复用 `game001` 的锁轴视图。
4. 补齐 game002 的环境配置、资产清单、场景转换、动画缩放、金额格式化、live spin adapter、入口和 README。
5. 补齐 game002 单测，覆盖：
   - 资源清单与状态文件约束
   - env 默认值和 fail-fast 校验
   - facade 流程边界
   - game adapter spin/collect/final scene 校验
   - layout 坐标与尺寸
   - money formatter
   - scene 转换
   - source boundary：禁止直接引入 netcore/uiframeworks/logiccore，禁止复制 game001 锁轴视图
6. 修复共享 `packages/uiframeworks` 页面容器滚动问题：`.slot-ui-page` 从 `overflow: hidden` 改为 `overflow: clip`，避免高设计稿画布在 Spin 按钮聚焦后被浏览器自动滚动裁切；新增对应回归测试。
7. `pnpm-lock.yaml` 已同步新增 `apps/game002` workspace importer。

## 未更新 agents.md

本任务没有新增长期仓库协作规则、目录规范或基础脚本约束；现有 `agents.md` 已覆盖 gameframeworks facade、pnpm/turbo、apps 目录和任务报告规则，因此未修改 `agents.md`。

## 严格验收记录

### 视觉对齐复验修正

用户截图指出 game002 symbol 未按第二套资源的 `40%` 缩放对齐。复查后确认初版实现误用了任务 49 计划中的旧建议 `0.3`，而 `apps/symbolsviewer` 已验证 `symbols002` 的固定缩放为 `0.4`。已将 `apps/game002/src/symbol-animation-config.ts` 同步为 `0.4`，并新增单测锁定所有 `GAME002_DISPLAY_SYMBOLS` 都使用 40% 显示缩放。

第二轮截图复验继续指出起点和 symbol 间隔仍未对齐。用 `assets/game002/bg.jpg` 叠加网格线后确认，计划里的 `200,400,900,1350 / 150px` 是旧假设；真实美术棋盘为 `x=200, y=330, width=720, height=1080`，即 `6 x 9` 的 `120px` pitch。已同步修改 `apps/game002/src/game-layout.ts`、layout 测试、adapter/runtime snapshot 测试和 README。

受影响包验收：

- `pnpm --filter game002 lint`：通过
- `pnpm --filter game002 test`：通过，9 个测试文件，37 个测试；覆盖率 All files `92.58 / 84.71 / 91.89 / 92.58`
- `pnpm --filter game002 typecheck`：通过
- `pnpm --filter game002 build`：通过，产物包含 `bg.jpg` 与 `symbols002` normal/spinBlur/disabled 资源
- `pnpm --filter @slotclientengine/uiframeworks test`：通过，9 个测试文件，45 个测试
- `pnpm --filter @slotclientengine/uiframeworks lint`：通过
- `pnpm --filter @slotclientengine/uiframeworks typecheck`：通过
- `pnpm --filter @slotclientengine/uiframeworks build`：通过

仓库级验收：

- `pnpm lint`：通过，22 个 package 全部成功
- `pnpm test`：通过，22 个 package 全部成功；其中 `game002` 37 个测试通过，`game001` 47 个测试通过
- `pnpm typecheck`：通过，22 个 package 全部成功
- `pnpm build`：通过，22 个 package 全部成功
- `git diff --check`：通过，无 whitespace 问题

浏览器验收：

- dev server：`pnpm --filter game002 dev -- --host 127.0.0.1 --port 5206`
- 验收 URL：`http://127.0.0.1:5206/`
- 初始加载：
  - title：`game002`
  - `canvas` attribute：`1125 x 2000`
  - 视口内缩放尺寸：`405 x 720`
  - `.slot-ui-page`：`overflow=clip`，`scrollTop=0`
  - `.slot-ui-frame` / `canvas`：`top=0`
  - console warn/error：无
- 点击唯一 `.slot-ui-spin-button` 后：
  - Spin 完整回到 `Ready`
  - `frameDataset.slotSpinState=idle`
  - 页面文本包含 `Balance$1,996.55`、`Win$5.40`、`Bet$0.05`
  - `.slot-ui-page` 仍为 `overflow=clip`，`scrollTop=0`
  - `.slot-ui-frame` / `canvas` 仍为 `top=0`
  - console warn/error：无
  - 已截取视口截图确认画面未被上移裁切

## 产物与清理

- 已清理验证产生的 `apps/game002/dist`、`apps/game002/coverage`、`apps/game002/.turbo`。
- `apps/game002/node_modules` 是 `pnpm install` 后的本地 workspace 链接环境，属于忽略目录，不作为源码交付面。

## 最终差异摘要

- 新增：`apps/game002` 应用源码、测试、配置和 README。
- 修改：`packages/uiframeworks/src/styles.css`、`packages/uiframeworks/tests/dom.test.ts`。
- 修改：`pnpm-lock.yaml`，新增 `apps/game002` importer。
- 新增：本执行报告。
