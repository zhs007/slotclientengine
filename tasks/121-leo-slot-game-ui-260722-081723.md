# 任务 121 执行报告：Leo Slot Game UI

- 执行时间（UTC）：2026-07-22 08:17:23
- 任务计划：`tasks/121-leo-slot-game-ui.md`
- 工作分支：`codex/task-121-leo-slot-game-ui`
- 基线提交：`aac9dba`（Task 120 injectable slot UI factory）
- 工具链：Node.js `v24.14.0`（`/Users/zerro/.nvm/versions/node/v24.14.0/bin/node`）、pnpm `10.0.0`（`/Users/zerro/.nvm/versions/node/v24.14.0/bin/pnpm`）
- 浏览器验收：待用户执行；按用户要求，本次没有启动或操作浏览器

## 完成内容

### 中立 frame host

- 在 `@slotclientengine/uiframeworks` 中公开 `createSlotUiFrameHost()`，统一创建同步且稳定的 page、frame、game layer、overlay host。
- 现有默认 DOM UI 改为复用该 host，原有 HUD 结构和事件合同保持不变。
- frame host 继续使用共享 viewport 算法，覆盖 fixed、focus、orientation-focus、maximized-focus，并统一负责 resize、unsubscribe 与幂等 destroy。
- 新增 host 稳定性、多实例隔离、viewport 更新和清理测试。

### 独立 Leo UI package

- 新增 `@slotclientengine/game-ui-leo`，以 public package entry 导出 `createLeoSlotGameUiFactory()`，样式通过 `@slotclientengine/game-ui-leo/styles.css` 显式导入。
- 每个 UI instance 独占 React root 和 external store；factory 同步返回稳定 host，不使用全局 singleton、DOM 查询或 body 状态。
- UI 只消费 framework snapshot，并只调用 typed commands；session、round、presentation、collect、balance 和 destroy 仍由 framework 独占。
- 实现第一版 contract 范围内的 balance、win、bet、bet +/-、spin、sound、fast、auto、状态禁用、错误展示与时钟。
- 未引入 test 分支的 framework/stateData/netcore2/event bridge，也未伪造 buy feature、autoplay scheduler、modal、paytable 或平台服务。
- 样式全部 namespace 到 `slot-leo-ui-*`，依赖 logical frame host 尺寸变量，不使用相对浏览器的 fixed/vw/vh 布局。

### game002 / game003 接入边界

- game002 仅在 `game-entry.ts` 的正式 100% 入口导入 Leo factory 和 CSS，并通过 Task 120 的 `uiFactory` 合同显式注入。
- game002 的 `main.ts`、loading resources 与 loading packages 未引入 React 或 game UI；Leo Loading 生命周期和 live session 准备路径未修改。
- game002 的玩法、adapter、rendercore、skin/lines/server 合同未修改。
- game003 保留默认 UI，未增加 `game-ui-leo`、React 或 ReactDOM 依赖；发布检查增加 React/Leo 污染防线。

## 资产白名单

只复制了 test 分支 Leo 游戏 HUD 所需的 2 个字体和 8 个控件图片，构建时以独立文件原字节输出；没有宽泛 glob、额外 hover 资源、Loading 资源、Wildsheep 资源或占位 fallback。

| 资产                                      |    字节 | SHA-256                                                            |
| ----------------------------------------- | ------: | ------------------------------------------------------------------ |
| `font/Anton-Regular.woff2`                |  56,336 | `088981f9b3682315d8610b2d3d05475e244f5243844f29df2fbfdbcf8eaf1ed9` |
| `font/NotoSansR.woff2`                    | 156,884 | `16bb3d8fb5c371c9e4fa6b5f313c0a5e2edd911c0ce6d0f9c3cee01e9560a2b3` |
| `controls/addbet.png`                     |   4,235 | `c7c3a944a3dc7f2633c017b764a0bf1e0ad35969132750aa0372672eb7d977fb` |
| `controls/image-autoplay.png`             |   2,305 | `feef678ca7d3f4399bd44f2b58332c51bce6920251f62c46834af7572091c4d7` |
| `controls/image-background-music-off.png` |   1,478 | `88906c0ed7b1faa9e9e79b58c8ee575a951a244a2cdc34187b6c619630606c72` |
| `controls/image-background-music.png`     |   1,460 | `072ec90b174990f343d27252bcb4480b3cda9744cc37ed146ce234a441414e3e` |
| `controls/image-fasplays-off.png`         |   1,300 | `6ef2b8e009780f53e8f99ed38d35597f1d7bbca0fec8098a9c405d33fe45a7e4` |
| `controls/image-fastplays.png`            |   1,253 | `c911b7f6032e6e8d4ecc7ba3cd40cf715f2b74e9dfd3e85cec356beef237075f` |
| `controls/image-play.png`                 |   5,563 | `10f91817d004f345b6b813603ae5726e04d804cae099ca6c4fbfd9e652b0c9ff` |
| `controls/removbet.png`                   |   4,134 | `75ad215da7f2c607d88ee7cb10a95a44d1456f148bc687d5c7f383584c13bd6c` |

## 自动化验收

按计划使用同一 Node 24 / pnpm 环境，game002 与 game003 串行执行，避免共享 package dist 竞争。

| 范围                               | 格式 | lint | typecheck | 测试                                                                                     | build / release      |
| ---------------------------------- | ---- | ---- | --------- | ---------------------------------------------------------------------------------------- | -------------------- |
| `@slotclientengine/uiframeworks`   | 通过 | 通过 | 通过      | 10 files / 56 tests；statements 95.01%、branches 90%、functions 91.87%、lines 95.23%     | build 通过           |
| `@slotclientengine/gameframeworks` | 通过 | 通过 | 通过      | 11 files / 48 tests；statements 91.51%、branches 82.07%、functions 96.87%、lines 91.70%  | build 通过           |
| `@slotclientengine/game-ui-leo`    | 通过 | 通过 | 通过      | 5 files / 15 tests；statements 97.56%、branches 98%、functions 96.66%、lines 100%        | build 通过           |
| `game002`                          | 通过 | 通过 | 通过      | 18 files / 97 tests；statements 86.34%、branches 80.33%、functions 92.41%、lines 87.09%  | `release:check` 通过 |
| `game003`                          | 通过 | 通过 | 通过      | 27 files / 136 tests；statements 92.52%、branches 83.68%、functions 95.72%、lines 92.48% | `release:check` 通过 |

另外：

- `pnpm install` 成功，lockfile 只增加新 workspace 依赖与 React 19.2.8 / ReactDOM 19.2.8 解析。
- source-boundary 测试覆盖依赖方向、禁用 API/语义、无全局 DOM/store、资源精确白名单、loading React-free、game003 React-free 和 timer/listener destroy path。
- Leo 与真实 gameframeworks 的集成测试覆盖 connect、idle、同一帧双击 spin 只触发一轮、spinning、presenting、collecting、回到 idle、余额更新和完整清理。
- game002 静态发布检查证明每个白名单资产只出现一次且字节一致；React/Leo 只存在于一个 `game-entry-*.js`。
- game003 静态发布检查证明全部 JS 不包含 Leo root、ReactDOM、React production error 或 `useSyncExternalStore` 标记。
- `git diff --check` 通过；生成配置和 symbol value resources 均无漂移。
- 构建仅保留仓库已有的 Vite 大 chunk 警告，以及 game003 生成器的 Node loader/deprecation 警告；无验收失败。

## Bundle 审计

### 独立 package

| 文件                                   | 原始字节 |            gzip 字节 |
| -------------------------------------- | -------: | -------------------: |
| `packages/game-ui-leo/dist/index.js`   |    6,191 |                2,013 |
| `packages/game-ui-leo/dist/styles.css` |    5,681 |                1,584 |
| 字体与图片白名单合计                   |  234,948 | 不适用（保持原文件） |

### game002

| 产物                         | Task 120 基线 |                      Task 121 |       变化 |
| ---------------------------- | ------------: | ----------------------------: | ---------: |
| initial loading `index-*.js` |     180,964 B |    180,964 B（gzip 33,687 B） |        0 B |
| formal `game-entry-*.js`     |   1,405,975 B | 1,631,681 B（gzip 346,795 B） | +225,706 B |
| formal `game-entry-*.css`    |       6,583 B |      11,312 B（gzip 2,912 B） |   +4,729 B |

结论：React、ReactDOM 与 Leo HUD 只随 100% 后的正式 `game-entry` 加载；initial loading chunk 体积未变化，也没有 React/Leo 标记。game003 发布包完全未包含 React/Leo。

## 浏览器验收交接

状态：**待用户验收**。按用户要求，本次未启动浏览器、未连接 live server、未填写人工结果，也未生成浏览器截图。

请使用合法 game002 URL 逐项确认：

1. Leo Loading 立即出现，进入正式游戏前无 React HUD 闪现。
2. 100% 后 Leo HUD 与 Pixi 游戏同时进入，frame 无跳位。
3. balance、win、bet 格式正确。
4. bet `+/-` 边界正确。
5. 连点 spin 不产生第二轮。
6. spinning、presenting、collecting 期间按钮状态正确。
7. sound、fast、auto pressed 状态正确。
8. error 信息可见。
9. 横竖屏和窗口 resize 后 HUD、canvas、focus region 对齐。
10. 控件以外区域不阻断 Pixi 点击。
11. destroy/re-enter 不残留旧 React UI、timer 或 listener。
12. Network 面板只有一条预期 live session 连接。
13. Console 无未处理 Promise、React cleanup、重复 key 或 hydration 警告。

同时抽查 game003，确认默认 HUD 和横竖屏行为未改变。

## 已知边界

- 浏览器视觉、live 连接和真实交互验收尚未执行，最终人工结论由用户补充。
- 本任务只实现 Task 120 contract 已支持的 Leo HUD；Wildsheep、平台切换、buy feature、自动轮次调度、设置持久化和 modal/paytable 均不在范围内。
- game002 formal entry 仍是既有的大型单 chunk；本次严格保持 loading closure 不变，未额外改写应用级拆包策略。
