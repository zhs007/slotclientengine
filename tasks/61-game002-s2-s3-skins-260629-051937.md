# game002 s2/s3 skins 执行报告

## 1. 基本信息

- UTC 时间戳：260629-051937
- 执行人：Codex
- 对应计划：`tasks/61-game002-s2-s3-skins.md`
- 执行结论：未完成，停在前置资产门禁。

## 2. 前置门禁结论

按计划先执行资源盘点和共享配置检查，当前 `assets/game002-s2/CN.png` 仍不存在，因此不能继续把 `skin=4` 接入为运行时支持。

原因：

- `assets/gamecfg002/gameconfig.json` 的 symbol code 包含 `CN=8`。
- `reels-001` 公开轮带实际出现 code `8`。
- 本任务要求 `skin=4` 复用 `assets/gamecfg002/gameconfig.json` 和同一套本地公开轮带。
- 因此 `CN` 是 `skin=4` 的运行必需贴图，不能用 `BN`、其它 skin 的 `CN`、空纹理或 fallback 替代。

已确认 `assets/game002-s3/CN.png` 存在，但这不能补足 `assets/game002-s2` 的资源合同。

## 3. 资源盘点

当前新增资源：

```text
assets/game002-s2/CO.png
assets/game002-s2/H1.png
assets/game002-s2/H2.png
assets/game002-s2/L1.png
assets/game002-s2/L2.png
assets/game002-s2/L3.png
assets/game002-s2/L4.png
assets/game002-s2/WL.png
assets/game002-s2/bg.png

assets/game002-s3/AF.png
assets/game002-s3/CM.png
assets/game002-s3/CN.png
assets/game002-s3/CO.png
assets/game002-s3/H1.png
assets/game002-s3/H2.png
assets/game002-s3/L1.png
assets/game002-s3/L2.png
assets/game002-s3/L3.png
assets/game002-s3/L4.png
assets/game002-s3/WL.png
assets/game002-s3/WM.png
assets/game002-s3/bg.jpg
```

尺寸检查：

- `assets/game002-s2/bg.png`：2000 x 2000
- `assets/game002-s3/bg.jpg`：2000 x 2000
- `assets/game002-s2/*.png` 中现有 symbol：均为 200 x 200
- `assets/game002-s3/*.png` 中现有 symbol：均为 200 x 200

未生成：

- `assets/game002-s2/*.spinBlur.png`
- `assets/game002-s2/*.disabled.png`
- `assets/game002-s2/symbol-state-textures.manifest.json`
- `assets/game002-s3/*.spinBlur.png`
- `assets/game002-s3/*.disabled.png`
- `assets/game002-s3/symbol-state-textures.manifest.json`

未生成原因：计划要求补齐 `assets/game002-s2/CN.png` 后再生成两套 manifest，避免产生一套不可通过最终验收的半成品。

## 4. skin 配置结论

用户强调新的 skin 和以前的 `skin=2` / `skin=3` 应该配置一样。恢复执行时应显式写入以下配置，而不是依赖默认值或隐式 fallback：

- `skin=4` 使用与 `skin=2` / `skin=3` 相同的 `GAME002_DEFAULT_GRID_LAYOUT`。
- `skin=5` 使用与 `skin=2` / `skin=3` 相同的 `GAME002_DEFAULT_GRID_LAYOUT`。
- `skin=4` 的 focus region 应与 `skin=2` / `skin=3` 一致：`x=637.5, y=330, width=720, height=1080`。
- `skin=5` 的 focus region 应与 `skin=2` / `skin=3` 一致：`x=637.5, y=330, width=720, height=1080`。
- 即使数值相同，也必须在 `game-layout.ts` / `skin-config.ts` 逐 skin 显式引用。

本次未修改运行时代码，因此没有新增 `skin=4` / `skin=5` 运行时映射。

## 5. 文档和协作规则

已检查：

- `AGENTS.md` 与 `agents.md` 当前一致。

本次未更新：

- `apps/symbolsviewer/README.md`
- `apps/game002/README.md`
- `AGENTS.md`
- `agents.md`

原因：任务未通过前置资源门禁，不能把 `skin=4` / `skin=5` 作为已完成长期规则写入协作约束。

## 6. 已执行命令

```bash
git status --short --untracked-files=all
```

结果：仅看到新增未跟踪资源和任务计划/本报告，无已跟踪文件修改。

```bash
git diff --stat
```

结果：无输出。

```bash
find assets/game002-s2 assets/game002-s3 -maxdepth 1 -type f -print | sort
```

结果：见“资源盘点”。

```bash
file assets/game002-s2/bg.png assets/game002-s3/bg.jpg assets/game002-s2/WL.png assets/game002-s3/WL.png
```

结果：背景均为 2000 x 2000，WL 均为 200 x 200。

```bash
file assets/game002-s2/*.png assets/game002-s3/*.png assets/game002-s3/*.jpg
```

结果：现有 symbol 均为 200 x 200，背景尺寸符合计划。

```bash
node -e 'const cfg=require("./assets/gamecfg002/gameconfig.json"); console.log(Object.entries(cfg.symbolCodes).map(([k,v])=>`${k}=${v}`).join(",")); console.log([...new Set(cfg.reels["reels-001"].flat())].sort((a,b)=>a-b).join(","));'
```

结果：

```text
WL=0,H1=1,H2=2,L1=3,L2=4,L3=5,L4=6,WM=7,CN=8,CM=9,CO=10,AF=11,BN=12
0,1,2,3,4,5,6,8
```

```bash
cmp -s AGENTS.md agents.md
```

结果：通过，两个文件一致。

```bash
find assets/game002-s2 assets/game002-s3 -maxdepth 1 -type f \( -name '*.spinBlur.png' -o -name '*.disabled.png' -o -name 'symbol-state-textures.manifest.json' \) -print | sort
```

结果：无输出，尚未生成状态图和 manifest。

```bash
git diff --check
```

结果：通过，无输出。

## 7. 未执行的验收命令

以下命令未执行，因为前置门禁失败，继续执行会验证一个已知不完整的实现：

```bash
pnpm --filter @slotclientengine/rendercore test
pnpm --filter symbolsviewer test
pnpm --filter symbolsviewer typecheck
pnpm --filter symbolsviewer lint
pnpm --filter symbolsviewer build
pnpm --filter game002 test
pnpm --filter game002 typecheck
pnpm --filter game002 lint
pnpm --filter game002 build
pnpm --filter game002 release:check
pnpm format:check
```

浏览器 live 验收未执行，原因是运行时接入未开始，且缺少 `skin=4` 必需资源 `assets/game002-s2/CN.png`。

## 8. 恢复执行步骤

补齐以下资源后恢复执行：

```text
assets/game002-s2/CN.png
```

恢复后先重新跑门禁：

```bash
find assets/game002-s2 assets/game002-s3 -maxdepth 1 -type f -print | sort
file assets/game002-s2/*.png assets/game002-s3/*.png assets/game002-s3/*.jpg
node -e 'const cfg=require("./assets/gamecfg002/gameconfig.json"); console.log(Object.entries(cfg.symbolCodes).map(([k,v])=>`${k}=${v}`).join(",")); console.log([...new Set(cfg.reels["reels-001"].flat())].sort((a,b)=>a-b).join(","));'
```

确认 `CN.png` 存在后，再生成状态图和 manifest：

```bash
pnpm --filter @slotclientengine/rendercore generate:symbol-state-textures -- --input-dir assets/game002-s2 --output-dir assets/game002-s2 --symbols WL,H1,H2,L1,L2,L3,L4,CN,CO --scale 1
pnpm --filter @slotclientengine/rendercore generate:symbol-state-textures -- --input-dir assets/game002-s3 --output-dir assets/game002-s3 --symbols WL,H1,H2,L1,L2,L3,L4,WM,CN,CM,CO,AF --scale 1
```

然后继续执行计划 5.3 到 9 的全部实现和验收。

## 9. 最终工作区摘要

`pnpm-lock.yaml` 未变化。

当前仍有未跟踪输入资产和任务文件：

```text
assets/game002-s2/*
assets/game002-s3/*
tasks/61-game002-s2-s3-skins.md
tasks/61-game002-s2-s3-skins-260629-051937.md
```

## 10. 二次遗漏检查

- 已确认 `assets/game002-s2/bg.png` 是背景，不应进入 symbol manifest。
- 已确认 `assets/game002-s2/CN.png` 缺失，因此未生成 manifest。
- 已确认 `assets/game002-s3/CN.png` 存在，但不能跨 skin 借用。
- 已确认未放宽 production fail-fast 合同。
- 已确认未修改 live/server/gamecode/bet/collect 合同。
- 已确认 `AGENTS.md` 和 `agents.md` 当前一致。
