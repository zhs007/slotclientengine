{
  "name": "gamelayouteditor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "prepare:deps": "pnpm --filter @slotclientengine/editorresource build && pnpm --filter @slotclientengine/rendercore build",
    "dev": "pnpm run prepare:deps && sh -c 'if [ \"$1\" = \"--\" ]; then shift; fi; exec vite \"$@\"' sh",
    "build": "pnpm run prepare:deps && vite build",
    "acceptance:task114": "TS_NODE_PROJECT=tsconfig.json node --loader ts-node/esm scripts/build-task114-acceptance.ts",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "typecheck": "pnpm run prepare:deps && tsc -p tsconfig.json --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@slotclientengine/browserartifactio": "workspace:*",
    "@slotclientengine/editorresource": "workspace:*",
    "@slotclientengine/rendercore": "workspace:*",
    "pixi.js": "^8.1.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitest/coverage-v8": "^4.0.0",
    "eslint-config-prettier": "^9.1.0",
    "fflate": "^0.8.3",
    "globals": "^15.15.0",
    "happy-dom": "^20.0.10"
  }
}
